# Copy to .env.shared and replace the token placeholder before shared deployment.
# This file is a template only; never commit a real token.
APP_ENV=production
DATA_DIR=/app/data
API_AUTH_REQUIRED=true
# Legacy API_AUTH_TOKEN is deliberately disabled in protected deployments.
API_AUTH_TOKEN=
# Inject a JSON object through the secret manager. Each identity requires role,
# actor, and tenant_id or tenant_ids, for example:
# {"replace-token":{"role":"admin","actor":"release-operator","tenant_id":"tenant-a"}}
API_AUTH_TOKENS=
API_AUDIT_ENABLED=true
API_DEFAULT_TENANT_ID=tenant-a
TENANT_STORAGE_ISOLATION=true

# The Streamlit UI is local-only. Keep it outside the protected API deployment.
STREAMLIT_APP_ENV=local
STREAMLIT_API_AUTH_REQUIRED=false
STREAMLIT_TENANT_STORAGE_ISOLATION=false
